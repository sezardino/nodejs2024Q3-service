version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: prisma_postgres_prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_prod

  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: app
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    env_file:
      - .env
    networks:
      - app_prod

volumes:
  postgres_data:

networks:
  app_prod:
    driver: bridge
